import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myappionicc.app',
  appName: 'my-app-ionic  ',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
